export enum EButtonSizes {
	SMALL = 'small',
	MEDIUM = 'medium',
	LARGE = 'large',
}

export enum EButtonTypes {
	FILLED = 'filled',
	PRIMARY = 'primary',
}

export enum EButtonShapes {
	SMOOTH = 'smooth',
	CIRCLE = 'circle',
}

export enum EButtonWaves {
	HOVER = 'hover',
	ACTIVE = 'active',
	PERMANENT = 'permanent',
	NONE = 'none',
}
